/**
 * Authorization check stubs — pass-through for now.
 * Structure is in place for future rules.
 */
export function checkAccess(context) {
  // Phase 1: checkUrlAccess(context)        — pass-through
  // Phase 2: checkCollectionAccess(context)  — pass-through
  // Phase 3: checkRecordAccess(context, query) — pass-through
}
